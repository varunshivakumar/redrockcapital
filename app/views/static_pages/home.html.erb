<% if signed_in? %>
    <div class="center hero-unit">
      <h1>Welcome <%= current_user.name %></h1>

      <h2>
        This is the home page for the RedRock Capital
      </h2>

      <div id="#container" data-highcharts-chart="1"></div>
    </div>

<% else %>
  <div class="center hero-unit">
    <h1>Welcome to the RedRock Capital</h1>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>
  </div>

<% end %>